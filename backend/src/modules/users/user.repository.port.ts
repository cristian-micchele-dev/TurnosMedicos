import {User} from './domain/user'; export interface UserRepository { findByEmail(email:string):Promise<User|undefined>; findById(id:string):Promise<User|undefined>; findAll(options?: { skip?: number; take?: number }):Promise<[User[], number]>; save(user:User):Promise<User>; update(user:User):Promise<void>; }
