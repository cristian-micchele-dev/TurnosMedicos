import {Role,User} from './user'; describe('User',()=>it('expone identidad sin hash',()=>{const u=new User('1','a@b.com','','secret',Role.DOCTOR);expect(u.publicia()).toEqual(expect.objectContaining({email:'a@b.com',role:Role.DOCTOR}));expect(u.publicia()).not.toHaveProperty('passwordHash');}));
