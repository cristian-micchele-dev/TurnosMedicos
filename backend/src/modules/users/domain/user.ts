export enum Role { ADMIN='ADMIN', DOCTOR='DOCTOR', PATIENT='PATIENT' }
export class User { constructor(public readonly id:string, public email:string, public name:string='', public passwordHash:string, public role:Role=Role.PATIENT, public active=true, public readonly createdAt=new Date()){} publicia(){return {id:this.id,email:this.email,name:this.name,role:this.role,active:this.active,createdAt:this.createdAt};} }
