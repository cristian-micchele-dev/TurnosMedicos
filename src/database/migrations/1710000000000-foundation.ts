import { MigrationInterface, QueryRunner } from 'typeorm';
export class Foundation1710000000000 implements MigrationInterface {
  async up(q: QueryRunner) { await q.query(`CREATE TYPE user_role AS ENUM ('ADMIN','DOCTOR','PATIENT'); CREATE TABLE users (id uuid PRIMARY KEY,email varchar(320) NOT NULL UNIQUE,password_hash text NOT NULL,role user_role NOT NULL DEFAULT 'PATIENT',active boolean NOT NULL DEFAULT true,created_at timestamptz NOT NULL DEFAULT now()); CREATE TABLE auth_sessions (id uuid PRIMARY KEY,user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,family_id uuid NOT NULL,token_hash text NOT NULL,jti text NOT NULL UNIQUE,expires_at timestamptz NOT NULL,revoked_at timestamptz,replaced_by uuid); CREATE TABLE password_reset_tokens (id uuid PRIMARY KEY,user_id uuid NOT NULL REFERENCES users(id) ON DELETE CASCADE,token_hash text NOT NULL,expires_at timestamptz NOT NULL,used_at timestamptz); CREATE INDEX auth_sessions_user_idx ON auth_sessions(user_id); CREATE INDEX auth_sessions_family_idx ON auth_sessions(family_id); CREATE INDEX reset_tokens_hash_idx ON password_reset_tokens(token_hash);`); }
  async down(q: QueryRunner) { await q.query('DROP TABLE password_reset_tokens; DROP TABLE auth_sessions; DROP TABLE users; DROP TYPE user_role;'); }
}
